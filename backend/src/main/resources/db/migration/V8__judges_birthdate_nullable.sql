-- Судья, заведённый из онлайн-заявки на категорию, на момент выдачи удостоверения
-- может не иметь даты рождения (кабинет её не собирает) — дозаполняется при аттестации.
ALTER TABLE judges ALTER COLUMN birth_date DROP NOT NULL;
