-- Судья из онлайн-заявки на категорию может не иметь пола в карточке на момент
-- выдачи удостоверения (кабинет его не собирает) — дозаполняется при аттестации.
ALTER TABLE judges ALTER COLUMN sex DROP NOT NULL;
