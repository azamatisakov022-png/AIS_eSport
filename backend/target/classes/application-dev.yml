spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  devtools:
    restart:
      enabled: true

logging:
  level:
    kg.gov.gafkis: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
